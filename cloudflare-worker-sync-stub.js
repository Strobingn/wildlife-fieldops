export default {async fetch(request, env){if(request.method==='OPTIONS')return new Response('',{headers:cors()});if(request.method!=='POST')return new Response('POST only',{status:405,headers:cors()});const body=await request.json();return new Response(JSON.stringify({ok:true,receivedQueue:body.queue?.length||0,at:new Date().toISOString()}),{headers:{...cors(),'content-type':'application/json'}})}};function cors(){return {'access-control-allow-origin':'*','access-control-allow-methods':'POST,OPTIONS','access-control-allow-headers':'content-type,authorization'}}