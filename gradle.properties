# Gradle settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:MaxMetaspaceSize=512m
org.gradle.caching=true
org.gradle.parallel=true

# Android settings
android.useAndroidX=true
android.nonTransitiveRClass=true
android.suppressUnsupportedCompileSdk=35

# Native libs: AGP 8.1+ always uses uncompressed libs by default
# (android.bundle.enableUncompressedNativeLibs was removed). 16 KB page
# zip-alignment is handled by AGP 8.5.1+ packaging + CameraX 1.4.2+.

# Kotlin settings
kotlin.code.style=official
