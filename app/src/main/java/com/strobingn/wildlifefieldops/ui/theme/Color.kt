package com.strobingn.wildlifefieldops.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand (cool field blue) ────────────────────────────────────────────────
// Names are retained for call-site compatibility across existing screens.
val PrimaryGreen = Color(0xFF5B9DFF)
val PrimaryGreenDark = Color(0xFF3B7AE8)
val PrimaryGreenLight = Color(0xFF9BC5FF)
val PrimaryContainer = Color(0xFF1A2F4A)
val OnPrimaryContainer = Color(0xFFD6E6FF)

// ── Dark application surfaces ─────────────────────────────────────────────
// Existing screens reference these legacy names directly, so these values are
// intentionally dark to apply the theme consistently throughout the app.
val BackgroundDark = Color(0xFF0B0D12)
val BackgroundCard = Color(0xFF141820)
val BackgroundElevated = Color(0xFF1B212B)
val SurfaceDark = Color(0xFF141820)
val SurfaceVariant = Color(0xFF222833)
val SurfaceBright = Color(0xFF2A3140)

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFF2F4F8)
val TextSecondary = Color(0xFFB6BECA)
val TextTertiary = Color(0xFF8D97A6)

// ── Status ─────────────────────────────────────────────────────────────────
val StatusPending = Color(0xFFFBBF24)
val StatusInProgress = Color(0xFF60A5FA)
val StatusCompleted = Color(0xFF5B9DFF)
val StatusCancelled = Color(0xFFFF6B6B)
val StatusUrgent = Color(0xFFFF6B6B)

// ── Accents ────────────────────────────────────────────────────────────────
val AccentBlue = Color(0xFF60A5FA)
val AccentPurple = Color(0xFFA78BFA)
val AccentOrange = Color(0xFFFB923C)
val AccentCyan = Color(0xFF38BDF8)
val AccentPink = Color(0xFFF472B6)
val AccentAmber = Color(0xFFFBBF24)

// ── Borders / chrome ───────────────────────────────────────────────────────
val BorderDark = Color(0xFF343D4C)
val DividerDark = Color(0xFF252C37)
val ScrimDark = Color(0xB3000000)

// ── Semantic ───────────────────────────────────────────────────────────────
val ErrorRed = Color(0xFFFF6B6B)
val ErrorRedDark = Color(0xFFE54B4B)
val SuccessGreen = Color(0xFF60A5FA)
val WarningYellow = Color(0xFFFBBF24)
val InfoBlue = Color(0xFF60A5FA)

// ── Gradients ──────────────────────────────────────────────────────────────
val GradientStart = Color(0xFF101722)
val GradientMid = Color(0xFF17263A)
val GradientEnd = Color(0xFF24518A)
