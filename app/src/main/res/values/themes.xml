<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main app theme — dark AppCompat parent keeps every screen dark regardless of
         the system light/dark setting (app ships dark-only). -->
    <style name="Theme.WildlifeFieldOps" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorPrimaryDark">@color/background_dark</item>
        <item name="colorAccent">@color/primary_green</item>
    </style>

    <!-- Splash screen theme — shown during cold start before Compose loads -->
    <style name="Theme.WildlifeFieldOps.Splash" parent="Theme.SplashScreen">
        <!-- Dark background matching the app -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- The icon to show on splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>

        <!-- Icon background tint -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>

        <!-- Maximum splash screen duration -->
        <item name="windowSplashScreenAnimationDuration">300</item>

        <!-- Post-splash theme -->
        <item name="postSplashScreenTheme">@style/Theme.WildlifeFieldOps</item>

        <!-- Match system bars -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
